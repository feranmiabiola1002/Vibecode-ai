services:
  - type: web
    name: vibe-backend
    runtime: python
    repo: https://github.com/yourusername/vibecode-ai
    branch: main
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_KEY
        sync: false
      - key: OPENAI_KEY
        sync: false
      - key: PAYSTACK_SECRET_KEY
        sync: false
      - key: PORT
        value: 10000

